## [2026-04-17 15:04] | Task: Converge app mode's Dock exposure

### 🤖 Execution Context
* **Agent ID**: `Codex`
* **Base Model**: `GPT-5`
* **Runtime**: `Codex CLI / Swift 6.2.4 / macOS`

### 📥 User Query
> `OpenCodexComputerUse` appears in the Dock during execution, which looks odd; aside from the permission popup page, everything else should run silently in the background.

### 🛠 Changes Overview
**Scope:** `apps/OpenCodexComputerUse`, `scripts/`, `docs/`

**Key Actions:**
- **[Converge app activation policy]**: Changed `PermissionOnboardingApp` from `.regular` to `.accessory`, preserving the permission window's visibility while no longer exposing the app to the Dock as an ordinary foreground application.
- **[Fix bundle metadata]**: Added `LSUIElement = true` to the `Info.plist` generated by the packaging script, so the process launched from the `.app` bundle also keeps agent-style behavior.
- **[Sync documentation]**: Updated the README, architecture doc, and permission onboarding execution plan to clarify the product boundary of "no Dock icon, permission window shown only on demand".

### 🧠 Design Intent (Why)
The goal of this change is not to hide the permission onboarding itself, but to make `OpenCodexComputerUse`'s runtime shape closer to a background automation service. Permission onboarding still needs an explicit window, but ordinary execution and resident bundle presence should not expose an extra icon in the Dock just because it's configured as a foreground `NSApplication`.

### 📁 Files Modified
- `apps/OpenCodexComputerUse/Sources/OpenCodexComputerUse/PermissionOnboardingApp.swift`
- `scripts/build-open-codex-app.sh`
- `README.md`
- `docs/ARCHITECTURE.md`
- `docs/exec-plans/active/20260417-permission-onboarding-app.md`
