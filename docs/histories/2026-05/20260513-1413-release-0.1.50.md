# Release 0.1.50

### Request

Commit and push the current OCU fixes, and cut a new release.

### Changes

- Bumped the public version from `0.1.49` to `0.1.50`.
- Synced the plugin manifest, Swift / Go runtime version constants, smoke init version, test fixture version, and the `computer-use-cli` example version.
- Updated the user-facing release note, describing this round's macOS click and input stability fixes.

### Motivation

This round of fixes resolves both Feishu / Lark Electron input and session-row click issues, and confirmed via side-by-side clicking on the official-build and dev-build Chrome/GitHub pinned repository that existing browser WebArea behavior was not broken. The user explicitly asked for the fixes to be committed and released, so per the release guide a separate version-bump commit and tag release was made.

### Files

- `plugins/open-computer-use/.codex-plugin/plugin.json`
- `packages/OpenComputerUseKit/Sources/OpenComputerUseKit/OpenComputerUseVersion.swift`
- `apps/OpenComputerUseSmokeSuite/Sources/OpenComputerUseSmokeSuite/main.swift`
- `packages/OpenComputerUseKit/Tests/OpenComputerUseKitTests/OpenComputerUseKitTests.swift`
- `apps/OpenComputerUseLinux/main.go`
- `apps/OpenComputerUseWindows/main.go`
- `scripts/computer-use-cli/main.go`
- `scripts/computer-use-cli/README.md`
- `docs/releases/feature-release-notes.md`

### Validation

- `swift test`
- `node ./scripts/npm/build-packages.mjs --out-dir dist/release/npm-staging-check`
- `node -p "require('./dist/release/npm-staging-check/open-computer-use/package.json').version"` returned `0.1.50`.
- Verified staging package includes `dist/linux/arm64/open-computer-use` and `dist/windows/arm64/open-computer-use.exe`.
- Verified staging root package has no `optionalDependencies`.
- `./scripts/build-cursor-motion-dmg.sh --configuration release --arch universal --version 0.1.50`
- `ls dist/release/cursor-motion/CursorMotion-0.1.50.dmg`
- `make check-docs`
