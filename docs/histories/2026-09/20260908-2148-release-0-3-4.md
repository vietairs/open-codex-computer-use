# Release 0.3.4

The user asked for a patch release after PR #61 and #62 were merged.

Unified the plugin manifest, Swift/Go runtimes, smoke/test client, and CLI sample versions to 0.3.4, and filled out both the Chinese feature release notes and the English GitHub Release notes. This release includes app-name-match ranking, custom secondary action naming, filter alignment, and a name-collision fix.

Verification: `swift test` — 161 tests, 0 failures, 1 Chrome live test skipped by default; Linux/Windows `go test ./...` passed; release-notes validation, doc checks, and diff checks passed. All three npm staging packages are at 0.3.4, including the macOS universal app, Linux/Windows arm64 and amd64 runtimes, and the ocu launcher; the universal `CursorMotion-0.3.4.dmg` built successfully. Public distribution is handled by the tag-triggered release workflow.
