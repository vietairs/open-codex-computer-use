## What's Changed

* Merge upstream `v0.3.5` into the fork, bringing SkyLight background click (`click_method: sky_click`), app-agent socket namespace isolation, real window-server drag delivery, secondary-action mapping fixes, and app-resolution ranking.
* Keep every fork-only capability working alongside the upstream changes: the lock-screen work guard and opt-in unattended policy, app-screen session validation, the menu bar status item, code-signature peer authentication on the app-agent socket, and the Stage Manager background click fallback.

**Full Changelog**: https://github.com/vietairs/open-codex-computer-use/compare/v0.2.1...v0.3.6-vietairs.1
