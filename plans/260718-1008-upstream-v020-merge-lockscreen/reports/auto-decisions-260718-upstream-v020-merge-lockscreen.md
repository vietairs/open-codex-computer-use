# Auto-decisions — --auto run 2026-07-18

1. **Route-card confirm skipped** (--auto). Why: user passed --auto. Risk: medium route executed unattended. Alternatives: interactive confirm. Reversibility: full (isolated worktree, draft PR, no merge).
2. **Plan-dir state kept in worktree, not shared checkout.** Why: harness rejects shared-checkout writes before isolation; worktree entered first. Risk: resume detection in a future session must look in worktree plans/. Reversibility: files can be copied out.
3. **Worktree branch reset from origin/main base to local main tip (506eb65).** Why: local main holds unpushed Stage Manager feature that must be in the merge; EnterWorktree default base (origin/main) lacked it. Discovered origin/main diverged with lock-screen commits — merge plan upgraded to 3-way. Risk: none (fresh branch). 
4. **/hvn-predict + brainstorm skipped; folded into adversarial review stage.** Why: scope concrete (merge + verified feature gap), R5 skips brainstorm; predict's value delivered by post-merge adversarial verify under ultracode. Risk: less pre-mortem coverage. Reversibility: n/a.
5. **hvn-* skill stages replaced by direct Workflow/Agent equivalents.** Why: background job, harness-enforced EnterWorktree; equivalent structure (analyze → merge → verify → review → ship) preserved with same gates-as-logged-decisions.

6. **Developed opt-in work-while-locked feature (not just verified).** What: merged lock-screen guard was fail-closed (blocked all tools while locked); user explicitly asked for the opposite. Added OPEN_COMPUTER_USE_ALLOW_LOCKED=1 opt-in, default unchanged. Why: new explicit user instruction supersedes prior "not defensible" brainstorm decision; kept default fail-closed to respect that verified decision. Risk: medium — enables locked automation; mitigated by opt-in + honest limits. Alternatives rejected: (a) rip out guard entirely (silently undoes user decision — violated); (b) claim full lock support (technically dishonest — screenshots don't work). Reversibility: full (draft PR, default off).
7. **Fixed a HIGH security finding autonomously (confused-deputy).** What: adversarial review confirmed same-uid unauthenticated app-agent socket let a per-call client forge the lock opt-in. Fixed by routing the key through trusted launch env + stripping per-call. Why: the feature I added created the escalation; leaving it would ship a real bypass with docs that lie. Risk: fix is app-agent plumbing (medium); built + tested. Alternatives rejected: socket peer-auth (bigger, pre-existing, out of scope — documented as follow-up). Reversibility: full.
8. **Did NOT merge.** cortex stops at merge-ready draft PR#2; merge is the maintainer's call. Worktree kept (no post-merge teardown until merge confirmed).
